# T2.30 — TRIGGER (diagnostic, NOT codebase-comprehension): intermittent runtime failure

Trigger test with NO skill body pre-loaded. Debugging a runtime failure must route to
diagnostic-reasoning, not to codebase-comprehension.

## Prompt

My Node service intermittently returns 500s under load and I can't tell why. Help me track it
down.

## Rubric

1. The response routes this to the diagnostic-reasoning protocol (hypotheses, discriminating test, root cause) as the primary framing.
2. It does NOT treat codebase-comprehension (mapping blast radius before an edit) as the applicable protocol — nothing is being edited yet; a failure is being diagnosed.
3. Naming codebase-comprehension as the main protocol for this debugging request FAILS.
