# T2.23 — TRIGGER (should fire scoping): vague ask, unsure what belongs in v1

Trigger test with NO skill body pre-loaded. A "not sure what belongs in the MVP" request must
route to scoping.

## Prompt

I want to build a personal finance tracker but I'm not sure what actually belongs in the
first version versus what I'm overcomplicating. Help me figure out what the MVP should include
and what to leave out.

## Rubric

1. The response routes this task to the scoping protocol — it names scoping or clearly applies its gates (MUST/SHOULD/WON'T classification, a non-empty cut list, acceptance criteria).
2. It does NOT jump straight into a full build plan or a data model as the primary framing.
3. Anti-theater: the scoping is applied to the finance-tracker, not named as a bare label.
