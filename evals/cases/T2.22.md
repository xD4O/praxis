# T2.22 — TRIGGER (should fire project-planning): settled goal, asking for build order

Trigger test with NO skill body pre-loaded — only the router is in context. A settled-scope
"give me the build order" request must route to project-planning.

## Prompt

I've decided to build a URL shortener as a weekend project — Go backend, Redis for storage,
custom domain. The scope and stack are settled. Give me the build plan: the steps in the
right order and what depends on what.

## Rubric

1. The response routes this task to the project-planning protocol — it either names project-planning as the applicable protocol or clearly applies its gates (per-task done-criteria, explicit dependencies, riskiest-work-first sequencing).
2. It does NOT treat the request as trivial (no-protocol) work.
3. Anti-theater: if it names project-planning, it actually applies it to the URL-shortener build, not as a bare label.
