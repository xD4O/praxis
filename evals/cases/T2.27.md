# T2.27 — TRIGGER (should NOT fire any new skill): trivial rename, over-trigger check

Trigger test with NO skill body pre-loaded. A trivial rename must be handled directly per the
complexity gate — it must NOT be routed into codebase-comprehension or any heavy protocol.

## Prompt

Rename the function `calc` to `calculateTotal` throughout my project — just tell me the
find-and-replace to run.

## Rubric

1. The response treats this as trivial mechanical work and answers directly (e.g. the sed/find-replace), per the router's complexity gate.
2. It does NOT route this into codebase-comprehension, project-planning, or any other heavy reasoning protocol as the framing for a simple rename.
3. Over-triggering — running a multi-gate protocol on a trivial rename — FAILS.
