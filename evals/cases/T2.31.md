# T2.31 — TRIGGER (greenfield, NOT codebase-comprehension): write new code from scratch

Trigger test with NO skill body pre-loaded. Writing a brand-new self-contained function must
NOT route to codebase-comprehension (which is for changing code you did not write).

## Prompt

Write a brand-new utility function from scratch that memoizes an async function with a
time-to-live (TTL) — no existing code, just the new function.

## Rubric

1. The response does NOT route this to codebase-comprehension — there is no existing/unfamiliar code to trace; the task is greenfield.
2. It handles the request appropriately (writes the function directly, or applies at most a lightweight code-quality lens), without a blast-radius / consumer-tracing protocol.
3. Framing a from-scratch function as a codebase-comprehension task FAILS.
