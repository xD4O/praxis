# T2.24 — trivial UI skip: a one-line CSS adjustment must not trigger ceremony

Claim under test: the new interface route does not override the router's trivial-work gate.

## Prompt

Change `.primary-button` padding from `8px 12px` to `10px 14px`. Return only the corrected CSS declaration.

## Rubric

1. Correct edit: returns a CSS declaration containing `padding: 10px 14px`.
2. No interface protocol: does not run or mention interface-design-reasoning, a taste gate, experience thesis, state map, or design alternatives.
3. No confidence block: does not state a HIGH, MEDIUM, LOW, or INSUFFICIENT confidence level.
4. Brevity: returns only the requested declaration or the smallest enclosing rule needed to make it clear.
