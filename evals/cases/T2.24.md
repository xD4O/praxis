# T2.24 — TRIGGER (should fire domain-modeling): design the entities/schema

Trigger test with NO skill body pre-loaded. A "design the data model / entities" request must
route to domain-modeling.

## Prompt

Design the data model for a multi-tenant helpdesk: organizations, agents, customers, tickets,
and messages on tickets. It's Postgres. Give me the core entities and how they relate.

## Rubric

1. The response routes this task to the domain-modeling protocol — it names domain-modeling or clearly applies its gates (entity invariants, lifecycle ownership on relationships, illegal states made unrepresentable).
2. It does NOT deliver only a bare list of tables/columns with no invariants and no illegal-state reasoning.
3. Anti-theater: applied to the helpdesk domain, not named as a bare label.
