# T2.26 — TRIGGER (should fire prompt-design): write a reusable production prompt

Trigger test with NO skill body pre-loaded. A "write a reusable prompt that runs automatically"
request must route to prompt-design.

## Prompt

Write the system prompt for a feature in my app that classifies incoming support tickets into
categories (billing, bug, feature-request, other) and returns the category as JSON. It runs
automatically on every ticket.

## Rubric

1. The response routes this task to the prompt-design protocol — it names it or clearly applies its gates (task spec, named failure modes, adversarial test inputs before shipping).
2. It does NOT hand over only a polished happy-path prompt with no failure-mode or adversarial-input reasoning.
3. Anti-theater: failure modes / tests are specific to ticket classification, not a bare label.
